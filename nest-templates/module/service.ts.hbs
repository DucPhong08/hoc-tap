import { Injectable } from '@nestjs/common';
import { BaseCrudService } from '@/infra/services/base-crud.service';
import { {{pascalCase (singularize name)}} } from '../entities/{{kebabCase (singularize name)}}.entity';
import { {{pascalCase (singularize name)}}Repository } from '../repositories/{{kebabCase (singularize name)}}.repository';

@Injectable()
export class {{pascalCase (singularize name)}}Service extends BaseCrudService<{{pascalCase (singularize name)}}> {
  constructor(
    private readonly {{camelCase (singularize name)}}Repository: {{pascalCase (singularize name)}}Repository,
  ) {
    super({{camelCase (singularize name)}}Repository, {
      notFoundMessage: 'Không tìm thấy {{kebabCase (singularize name)}}',
    });
  }
}
