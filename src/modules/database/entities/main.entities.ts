import { SettingEntity } from '../../../infra/settings/entities/setting.entity';
import { UserModel } from '../models/user.model';

export const MAIN_ENTITIES = [SettingEntity, UserModel];

export const MainEntities = MAIN_ENTITIES;
